/**
 * 评估语料 + 标注。
 *
 * ## 为什么语料要写死在代码里
 *
 * 评估必须可复现:同一套文档、同一批问题,才能比较"改动前后"。如果拿
 * data/rag-index.json 里用户自己的文档评,每次结果都不一样,指标失去意义。
 * 所以这里自带一份语料,评估时灌进一个独立的内存索引再跑。
 *
 * ## 语料规模不是随便定的
 *
 * 第一版只写了 4 篇文档、12 块,结果三种模式的 Recall 全是 100%。
 * 原因不是检索好,是 candidatePoolSize 就等于 12 —— 两路各自返回了整个
 * 语料库,RRF 只是在重排全集,**根本没有检索发生**。
 *
 * 评估集的块数必须显著大于候选池,否则测的是"排序",不是"检索"。
 * 现在是 14 篇 / 约 50 块,候选池 12,选择性才真正起作用。
 *
 * ## 干扰文档是这份语料的核心设计
 *
 * 只写"每个问题有唯一正确答案"的语料,评不出任何东西 —— 检索只要不瞎
 * 就全对。真正决定 RAG 质量的是**主题相邻但答案不同**的文档能不能区分开。
 * 所以这里刻意放了几组容易混的:
 *
 * - 「文档上传限制」vs「对话长度限制」:都在讲"超过上限会被拒绝",
 *   都有 4xx 状态码,区别只在主体是文档还是对话。
 * - 「切块参数说明」vs「向量与相似度」vs「embedding 模型选择」:
 *   三篇都在讲 RAG 内部机制,词汇高度重叠(向量、维度、模型、文本)。
 * - 「部署与端口」vs「开发环境配置」:都讲端口和代理。
 * - 「版本变更记录」里同时提到混合检索和评估集,和讲这两件事的专门文档
 *   构成竞争 —— 问"混合检索是什么"该命中原理文档,问"哪个版本引入的"
 *   该命中变更记录。这一组专门测语义区分,不是关键词能解决的。
 *
 * ## 两路各自的盲区
 *
 * - 向量的盲区:错误码 `ERR_UPLOAD_413`、版本号 `v2.3.1`、配置项名
 *   `chunk_overlap` —— 这些在语义空间里没有邻居,embedding 只看到一串字符。
 * - BM25 的盲区:用户问"上传大文件失败怎么办",文档写的是"体积超限
 *   会被拒绝" —— 一个字都不重合,纯关键词彻底抓瞎。
 *
 * 两类各占一部分,再加一批两路都能搞定的,才能看出混合检索是"补上了短板"
 * 还是"把本来对的搞坏了"。后者是真实风险 —— RRF 会让单路的强命中被
 * 另一路的噪声拉下来。
 */

import type { EvalCase } from './evaluate.ts'

export interface EvalDocument {
  title: string
  text: string
}

/**
 * 评估语料。刻意写成 Markdown 带标题层级,因为 chunker 会按标题切块,
 * 纯文本评不出标题路径对检索的贡献。
 */
export const evalDocuments: EvalDocument[] = [
  {
    title: '文档上传限制',
    text: `# 文档上传限制

## 体积上限

单篇文档的正文体积超过 20 万字符时会被拒绝，接口返回 413。
体积超限的判断发生在切块之前，所以超大文档不会浪费 embedding 算力。
这个顺序是刻意的：先校验再处理，失败路径的成本要尽量低。

## 错误码

上传失败时接口会返回具体错误码，便于前端给出针对性提示：

- ERR_UPLOAD_413：正文体积超过上限
- ERR_UPLOAD_EMPTY：正文为空或只有空白字符
- ERR_UPLOAD_PARSE：文件编码无法识别，通常是非 UTF-8 编码的文本

## 支持的格式

目前只接受纯文本类文件，也就是 .txt 和 .md 两种扩展名。
PDF 和 Word 需要额外的解析库，还没有支持。
二进制文件即使改了扩展名也会在解析阶段失败。
`
  },
  {
    title: '对话长度限制',
    text: `# 对话长度限制

## 条数与字符上限

单次请求携带的历史消息最多 40 条，超过直接拒绝，返回 400。
单条消息正文上限 2 万字符，整段对话累计上限 12 万字符。
三个上限任意一个被突破都会拒绝整个请求，不做截断。

## 为什么不截断

自动截掉最早的几轮听起来更友好，但会让模型看到一段被人为剪断的
对话，回答质量下降却没有任何提示。宁可明确拒绝，让调用方决定丢哪些。

## 超长时的表现

模型侧还有一层上下文窗口限制。如果请求通过了本地校验但超出模型窗口，
返回的 stop_reason 是 model_context_window_exceeded，前端会显示
"对话过长"而不是笼统的失败。
`
  },
  {
    title: '切块参数说明',
    text: `# 切块参数说明

## 目标长度

chunk_target 控制每块的目标字符数，默认 400。
这个值和 embedding 模型的输入上限挂钩：bge-small 的上限是 512 token，
中文大约一字一 token，留出余量后 400 是安全的。
超过 chunk_max（默认 600）的单句会被按字数硬切。

## 重叠

chunk_overlap 默认 60，指相邻两块之间重复的字符数。
留重叠是为了避免一句关键的话正好被切在边界上，两块都只拿到半句。
调大重叠会增加索引体积，因为同一段文字被存了两遍。

## 短块合并

短于 60 字符的块会被并进前一块，避免产生大量碎片。
碎片块的向量往往没有意义——太短的文本 embedding 不稳定。
合并只在标题路径相同的相邻块之间进行，跨章节不合并。
`
  },
  {
    title: '向量与相似度',
    text: `# 向量与相似度

## 维度

每个文本块被压成 512 个浮点数。维度是模型决定的，不能自己改。
换用维度不同的模型时，旧的索引数据必须整体重建，
因为不同维度的向量之间没有可比性。

## 归一化与点积

入库时向量已经做过归一化，长度为 1。
归一化之后余弦相似度就退化成点积，省掉每次查询时算模长的开销。
这是个纯粹的性能优化，不影响排序结果。

## 暴力扫描的代价

查询时对全部块逐个算点积，不建任何索引。
几百块的规模下 512 维扫 1000 块是 50 万次乘加，亚毫秒级。
到十万块量级才需要换带 HNSW 或 IVF 索引的实现。
`
  },
  {
    title: 'embedding 模型选择',
    text: `# embedding 模型选择

## 为什么放在本地跑

生成端点的中转服务不代理 embedding 接口，试过三个模型都返回 503。
所以只剩本地推理这一条路。代价是要带 24MB 权重、首次加载几百毫秒，
收益是零调用成本、数据不出本机。

## 非对称检索

bge 系列是非对称检索：查询侧要加一段指令前缀，文档侧不加。
加错不会报错，只会让检索质量悄悄变差，属于最难发现的一类问题。

## pooling 方式

bge 用 CLS token 做句向量，所以 pooling 必须设成 cls 而不是 mean。
设错同样不报错，向量空间对不上，检索结果明显变差但没有任何提示。
`
  },
  {
    title: '混合检索原理',
    text: `# 混合检索原理

## 两路互补

向量检索懂语义但丢字面精确性，关键词检索认字面但完全不懂语义。
专有名词、型号、错误码、代码标识符这类内容在语义空间里没有邻居，
只能靠字面匹配捞出来。所以两者是互补关系，不是替代关系。

## BM25 的三个部分

逆文档频率让罕见词权重更高，常见虚词自动被压到接近无效。
词频饱和让重复堆砌关键词无法持续加分。
长度归一化消除长文档天然更容易命中的偏差。

## RRF 融合

两路的分数量纲不同，不能直接加权求和。
倒数排名融合只用名次，彻底绕开量纲问题。
平滑常数取 60，作用是压制头部，让"两路都投票"比"单路排第一"更重要。
`
  },
  {
    title: '检索质量评估',
    text: `# 检索质量评估

## 三个指标

召回率回答"该找到的有几成找到了"，是整个流程的下限。
平均倒数排名回答"第一个正确结果排第几"。
归一化折损累积增益同时考虑找到几个和排在多前，最接近人的主观感受。

## 为什么三个都要看

召回率满分也可能排序很差：正确结果全挤在第三第四位时，
倒数排名只有三分之一，换个字符预算更紧的场景就会崩。
单看任何一个指标都会误判。

## 已知局限

语料规模小，统计效力弱。标注是人工写的，会不自觉地贴近文档用词。
只评检索不评生成，模型无视资料自己编这里测不出来。
相关性被简化成二元的，没有区分高度相关和部分相关。
`
  },
  {
    title: '版本变更记录',
    text: `# 版本变更记录

## v2.3.1

修复了删除文档后关键词索引没有同步重建的问题。
这个 bug 的表现很隐蔽：被删文档仍然能被关键词检索命中，
但向量检索里已经没有了，看起来像"有时搜得到有时搜不到"。

## v2.3.0

检索从纯向量改为混合检索，向量与 BM25 的结果用 RRF 融合。
同时新增检索过程的可视化面板。

## v2.2.0

引入检索质量评估集，指标包括召回率、平均倒数排名和折损累积增益。

## v2.1.0

流式渲染改为分阶段：生成中只渲染纯文本，结束后再渲染完整格式。
`
  },
  {
    title: '部署与端口',
    text: `# 部署与端口

## 端口配置

服务端默认监听 8787，通过环境变量 PORT 覆盖。
生产模式下服务端会托管前端构建产物，所以线上只需要开一个端口。

## 生产模式启动

启动前必须先执行构建，否则只会看到一堆 404。
服务端启动时会检查构建产物是否存在，缺失时打印明确的警告，
而不是让人对着 404 猜原因。

## 权重文件

embedding 权重不进版本库，体积 24MB，需要单独下载到本地目录。
权重缺失时服务照常启动，只是知识库功能不可用，普通对话不受影响。
`
  },
  {
    title: '开发环境配置',
    text: `# 开发环境配置

## 两个进程

开发时前端和服务端分开跑，前端在 5173，服务端在 8787。
用 concurrently 一条命令同时启动两个，避免开两个终端。

## 代理转发

前端开发服务器把 /api 前缀的请求转发到服务端。
这样浏览器看到的是同源请求，不会触发预检，也不用在服务端配任何跨域头。
上线后前后端同源，代理这层自然就不需要了。

## 类型检查

前端和服务端各有一份独立的类型配置，检查命令会把两份都跑一遍。
构建命令前置了类型检查，类型不过就不会产出构建结果。
`
  },
  {
    title: '流式传输机制',
    text: `# 流式传输机制

## 为什么用服务器推送事件

回复是单向的，服务端往浏览器推，不需要双向通道。
它走普通 HTTP，不用另开协议，也不用处理连接升级。
双向通道适合聊天室这种两边都要主动发消息的场景，这里用不上。

## 两个必踩的坑

网络分片和消息帧边界无关，一个分片可能只包含半个帧，
所以要把不完整的尾部留到下一个分片再拼。

一个中文字符占三个字节，字节边界可能落在字符中间。
解码时必须开启流式模式，让解码器把不完整的字节序列留到下次。

## 响应头

内容类型是事件流，缓存必须关掉，并且要禁止中间代理做任何转换，
否则代理的压缩缓冲会让流式变成一次性返回。
`
  },
  {
    title: '中止与重试',
    text: `# 中止与重试

## 中止的传播

浏览器断开连接时，服务端要主动中止对上游模型的请求。
不做这一步的话，用户关掉页面后上游还在继续生成，费用照算。

## 已收到内容的处理

用户主动停止时，已经显示出来的文字要保留，只把状态标成已停止。
让看着长出来的文字凭空消失比留着更奇怪。

## 重试复用

重试失败的回复时复用同一条消息，不新增气泡，
避免界面里堆积失败残影。重试会重新检索，所以旧的引用来源要清掉，
否则来源和新答案对不上。
`
  },
  {
    title: '提示注入防护',
    text: `# 提示注入防护

## 风险来源

检索到的文档是外部内容，可能藏着"忽略之前的指令"之类的话。
这类攻击不需要攻击者接触系统，只要往知识库里放一篇文档就能生效。

## 三层处理

第一层：资料用标签包裹，并在系统提示里声明标签内是数据不是指令。
第二层：转义资料里的尖括号，防止文档伪造一个闭合标签让模型
以为资料区提前结束，后面的内容逃逸成指令。
第三层：输出侧净化兜底。

## 第三层为什么必须存在

系统提示的优先级是训练出来的倾向，不是硬性机制，前两层都可能被绕过。
只有输出侧的净化是确定性的，不依赖模型是否听话。
`
  },
  {
    title: '持久化策略',
    text: `# 持久化策略

## 存在哪里

会话记录存在浏览器本地存储里，刷新页面不丢。
服务端不保存任何对话内容。

## 写入时机

生成过程中每个字都会改变状态，如果原样跟着写，一次回复就是
几百次序列化加写入。本地存储是同步接口，会直接卡住渲染。
所以做两层节流：防抖合并连续变更，再加一个兜底计时器保证最长间隔必写一次。

## 配额写满

写入随时可能失败，无痕模式下直接抛错，配额写满抛配额异常。
配额满时不是放弃，而是逐步减少保留的会话数再试——
存下最近的对话比一条都不存有用。

## 读取时的校验

本地存储的内容是不可信输入，用户能手改，旧版本也会留下结构不同的数据。
所以读取时逐字段校验，宁可丢掉一条坏消息。
`
  }
]

/**
 * 标注。
 *
 * tag 的含义:
 * - keyword: 问题里带精确标识符(错误码/版本号/配置项名),考 BM25
 * - semantic: 问法和文档用词不重合,考向量
 * - mixed: 两路都该能命中,用来检查融合有没有把对的搞坏
 *
 * 每条尽量只标一篇期望文档。标多篇会让 Recall 变成"找到几篇之一"的
 * 模糊指标 —— 语料里主题相邻的文档很多,标宽了等于放低标准。
 */
export const evalCases: EvalCase[] = [
  // ---- 考 BM25:精确标识符,向量检索的盲区 ----
  {
    question: 'ERR_UPLOAD_413 是什么意思',
    expectedDocuments: ['文档上传限制'],
    tag: 'keyword',
    note: '错误码在语义空间里没有邻居，纯向量容易排到「对话长度限制」上——那篇也讲超限和 4xx'
  },
  {
    question: 'chunk_overlap 默认是多少',
    expectedDocuments: ['切块参数说明'],
    tag: 'keyword',
    note: '配置项名是字面匹配，BM25 该秒中；向量容易混到「向量与相似度」'
  },
  {
    question: 'chunk_target 这个参数控制什么',
    expectedDocuments: ['切块参数说明'],
    tag: 'keyword'
  },
  {
    question: 'v2.3.1 修了什么',
    expectedDocuments: ['版本变更记录'],
    tag: 'keyword',
    note: '版本号被切成整词，测数字型标识符'
  },
  {
    question: 'v2.1.0 改了什么',
    expectedDocuments: ['版本变更记录'],
    tag: 'keyword'
  },
  {
    question: 'PORT 环境变量的作用',
    expectedDocuments: ['部署与端口'],
    tag: 'keyword',
    note: '「开发环境配置」也讲端口，靠 PORT 这个字面词区分'
  },
  {
    question: 'ERR_UPLOAD_PARSE 什么时候出现',
    expectedDocuments: ['文档上传限制'],
    tag: 'keyword'
  },

  // ---- 考向量:问法和文档用词不重合,BM25 的盲区 ----
  {
    question: '上传一个特别大的文件会怎样',
    expectedDocuments: ['文档上传限制'],
    tag: 'semantic',
    note: '文档写的是「体积超过上限会被拒绝」，和「特别大的文件」零字面重合'
  },
  {
    question: '为什么相邻的两块内容会重复一部分',
    expectedDocuments: ['切块参数说明'],
    tag: 'semantic',
    note: '考「重叠」这个概念的语义匹配'
  },
  {
    question: '前端和后端跨域问题是怎么解决的',
    expectedDocuments: ['开发环境配置'],
    tag: 'semantic',
    note: '文档提了代理和同源，但没写「跨域」这个词'
  },
  {
    question: '为什么很短的片段不单独存',
    expectedDocuments: ['切块参数说明'],
    tag: 'semantic'
  },
  {
    question: '用户关掉页面后还会继续计费吗',
    expectedDocuments: ['中止与重试'],
    tag: 'semantic',
    note: '文档说的是「不中止上游费用照算」，问法完全不同'
  },
  {
    question: '知识库里放一篇恶意文档能做什么',
    expectedDocuments: ['提示注入防护'],
    tag: 'semantic'
  },
  {
    question: '为什么不选双向通道',
    expectedDocuments: ['流式传输机制'],
    tag: 'semantic',
    note: '文档讲的是「回复是单向的」，没有「双向通道」以外的字面钩子'
  },
  {
    question: '刷新页面之后聊天记录还在吗',
    expectedDocuments: ['持久化策略'],
    tag: 'semantic'
  },
  {
    question: '一次能带多少轮历史',
    expectedDocuments: ['对话长度限制'],
    tag: 'semantic',
    note: '和「文档上传限制」构成干扰：两篇都在讲上限和拒绝'
  },

  // ---- 两路都该命中:检查融合有没有把对的搞坏 ----
  {
    question: '支持上传 PDF 吗',
    expectedDocuments: ['文档上传限制'],
    tag: 'mixed',
    note: 'PDF 既是字面词也有语义，两路都该进榜'
  },
  {
    question: '混合检索是哪个版本引入的',
    expectedDocuments: ['版本变更记录'],
    tag: 'mixed',
    note: '和「混合检索原理」竞争：问的是版本，该命中变更记录'
  },
  {
    question: 'RRF 融合为什么不用加权求和',
    expectedDocuments: ['混合检索原理'],
    tag: 'mixed',
    note: '和上一条互为镜像：问原理，该命中原理文档而不是变更记录'
  },
  {
    question: 'embedding 权重文件要怎么获取',
    expectedDocuments: ['部署与端口'],
    tag: 'mixed',
    note: '和「embedding 模型选择」竞争：问获取方式该命中部署文档'
  },
  {
    question: 'pooling 应该设成什么',
    expectedDocuments: ['embedding 模型选择'],
    tag: 'mixed'
  },
  {
    question: '本地存储配额写满了怎么办',
    expectedDocuments: ['持久化策略'],
    tag: 'mixed'
  },
  {
    question: '为什么向量要做归一化',
    expectedDocuments: ['向量与相似度'],
    tag: 'mixed',
    note: '和「embedding 模型选择」词汇高度重叠'
  },
  {
    question: 'nDCG 和召回率的区别',
    expectedDocuments: ['检索质量评估'],
    tag: 'mixed'
  }
]
